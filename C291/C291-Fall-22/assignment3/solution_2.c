#include <stdio.h>
int main(void){
	int number = 0;
	int count = 0; 
	char numstate = 't';
	int prime = 0;
	puts("Enter a positive number that you with to factor");
	scanf("%d",&primer);
	while(number < primer){
	while(count < number) {
		if(number % count == 0){
			numstate = 'f';
		} 
		if((prime % number) & (numstate = 't')) {
			printf("%d",number);

}
number++;
}
}
}
